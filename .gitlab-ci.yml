stages:
  - test
  - release
  - deploy

variables:
  BASE_URL: "https://api.meteo.cat"

before_script:
  - python -V  # Verificar versión de Python
  - python -m pip install --upgrade pip
  - pip install poetry  # Instalar Poetry
  - poetry config virtualenvs.create false  # Desactivar la creación de entornos virtuales
  - poetry install  # Instalar dependencias con Poetry

test_job:
  stage: test
  image: python:3.12.7  # Actualización a Python 3.12.7
  script:
    - poetry install  # Instalar dependencias con Poetry
    - python meteocat/locations.py  # Ejecuta tu script principal
  artifacts:
    paths:
      - municipis_result.json  # Archivo que queremos guardar
    expire_in: never  # No expira. El archivo generado estará siempre disponible
  only:
    - master

release_job:
  stage: release
  image: node:20  # Imagen de Docker para ejecutar Semantic Release
  script:
    - apt-get update -y  # Actualiza la lista de paquetes
    - apt-get install -y python3 python3-pip  # Instala Python 3 y pip
    - python3 --version  # Verifica que Python 3 esté instalado correctamente
    - pip3 --version  # Verifica que pip3 esté disponible
    - npm install -g semantic-release  # Instalar Semantic Release globalmente
    - semantic-release  # Ejecutar Semantic Release para crear una nueva versión
  only:
    - master  # Ejecutar sólo en la rama master

deploy_job:
  stage: deploy
  image: python:3.12.7  # Actualización a Python 3.12.7
  script:
    - echo "Desplegando la aplicación..."
    - python deploy.py  # Reemplaza con tu lógica de despliegue
  only:
    - tags
